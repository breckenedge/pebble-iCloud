# Development Environment Configuration

# Flask Environment
FLASK_ENV=development

# Database (SQLite for development)
DATABASE_PATH=users.db

# JWT Secret Key (development only - don't use in production!)
JWT_SECRET_KEY=dev_secret_key_change_in_production

# Encryption Key will be auto-generated in .encryption_key file for development

# Server Port
PORT=5000
